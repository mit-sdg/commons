version: 1
runtime: bun
packages:
  - .
  - frontend
scripts:
  build: build
  start: platform:start
port: 3000
health:
  path: /health
storage:
  bindings:
    default:
      type: mongo
      environment:
        uri: MONGODB_URI
