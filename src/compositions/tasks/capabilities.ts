/** The one role every member of a task list holds, in that list's own context. */
export const TASK_LIST_MEMBER_ROLE = "task-list-member";

/** Every member of a list holds the same full powers over its tasks. */
export const TASK_LIST_MEMBER_CAPABILITY = "tasks:manage";

export const TASK_LIST_MEMBER_CAPABILITIES = [TASK_LIST_MEMBER_CAPABILITY];
